class Solution:
    def getConcatenation(self, nums: List[int]) -> List[int]:
        ans = []
        i = 0

        for n in nums:
            ans.append(n)
        for n in nums:
            ans.append(n)
        return ans